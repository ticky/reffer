language: node_js
node_js:
  - node
sudo: false
before_install:
  - npm install -g yarn@^0.17.10
install:
  - yarn
script:
  - yarn lint
  - yarn test
after_success:
  - bash <(curl -s https://codecov.io/bash)
